"use client";import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {loginWithEmail,loginWithGoogle,resetPassword} from "@/lib/auth";export default function Login(){const r=useRouter();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function go(){try{await loginWithEmail(email,password);r.push('/dashboard')}catch(e:any){setMsg(e.message)}}async function google(){try{await loginWithGoogle();r.push('/dashboard')}catch(e:any){setMsg(e.message)}}return <main className="mx-auto max-w-md px-4 py-16"><div className="card"><h1 className="text-3xl font-black">Login</h1><div className="mt-6 space-y-4"><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn-primary w-full" onClick={go}>Login</button><button className="btn-secondary w-full" onClick={google}>Continue with Google</button><button className="text-sm text-greenx" onClick={()=>email&&resetPassword(email).then(()=>setMsg('Reset email sent'))}>Forgot password?</button><p className="text-sm text-white/60">No account? <Link className="text-greenx" href="/signup">Signup</Link></p>{msg&&<p className="text-sm text-white/70">{msg}</p>}</div></div></main>}
